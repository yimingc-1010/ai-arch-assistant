"""
Task definitions for the autocrawler scheduler.

TODO: Implement tasks:
- crawl_url(url): crawl a single URL and store result
- crawl_batch(urls): crawl multiple URLs
- scheduled_crawl(job_id): execute a scheduled crawl job
"""
